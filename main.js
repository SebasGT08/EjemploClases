

function showMenu() {
    // Obtener el elemento del menú desplegable
    
    var submenu = document.getElementById("submenu");
    
    // Cambiar la clase CSS del elemento para mostrar u ocultar el menú desplegable
    submenu.classList.toggle("show");
    console.log(submenu);
}